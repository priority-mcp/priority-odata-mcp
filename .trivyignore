# Trivy ignore list (one CVE/GHSA ID per line). Currently empty.
#
# checked 2026-09-11, fix applied: runtime base moved from nodejs22-debian12 (libssl3 3.0.18,
#   still unpatched in distroless) to gcr.io/distroless/nodejs22-debian13:nonroot (libssl3t64 3.5.7,
#   libc6 2.41). The six OpenSSL entries (CVE-2026-31789, -28387, -28388, -28389, -28390, -45447)
#   were removed; Trivy reports 0 HIGH/CRITICAL on the new image. See #3.
