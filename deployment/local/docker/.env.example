# Optional: copy to .env in this directory if you need compose-only overrides.
# Server credentials and Priority settings normally live in the repo-root .env
# (see env_file in docker-compose.yml: ../../../.env).
